<table class="table table-hover">
  <thead>
  <tr>
    <th></th>
    <th>Average</th>
  </tr>
  </thead>

  <tbody>
  <% tops.each do |t| %>
      <tr>
        <td><%= link_to t.name, t %></td>
        <td><%= t.average_rating %></td>
      </tr>
  <% end %>
  </tbody>
</table>
