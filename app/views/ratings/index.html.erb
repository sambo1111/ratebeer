<h2> List of ratings </h2>

Number of ratings: <%= @ratings.count %>

<ul>
  <% @ratings.each do |rating| %>
    <li> <%= rating.to_s %> <%= link_to rating.user.username, user_path(rating.user)%> </li>
  <% end %>
</ul>

<% if current_user %>
  <%= link_to 'New rating', new_rating_path %>
<% end %>
